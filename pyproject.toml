[project]
name = "rag_application"
version = "0.1.0"
requires-python=">=3.12"
dependencies = [
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
    "mypy",
    "pre-commit",
    "pylint"
]
